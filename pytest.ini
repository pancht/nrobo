[pytest]
markers =
    example: Example demo test — not executed in CI
    api: Api tests
    playwright: requires Playwright engine
    selenium: requires Selenium engine
filterwarnings =
    ignore::pytest.PytestAssertRewriteWarning
